spring:
  application:
    name: config-server
  cloud:
    config:
      fail-fast: false
      server:
        git:
          uri: https://github.com/daggerok/spring-cloud-examples
          search-paths: 02-config-server-zipkin-server/config-server/spring-config-repository
          force-pull: true
